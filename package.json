{
  "name": "roll-label",
  "version": "1.0.0",
  "description": "Util to create printable roll label",
  "type": "module",
  "bin": {
    "roll-label": "main.js"
  },
  "scripts": {
    "build": "esbuild app.jsx --jsx=automatic --outdir=dist",
    "dev": "esbuild app.jsx --jsx=automatic --outdir=dist --watch"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "esbuild": "^0.25.2",
    "prettier": "^3.5.3",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "yaml": "^2.7.1"
  }
}
